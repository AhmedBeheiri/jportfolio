name: shared
description: Shared business logic and data models for JPortfolio.
version: 0.0.1
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  backend_client:
    path: ../client
  serverpod_client: ^3.1.1 # Adjust version based on installed serverpod
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.1
  json_serializable: ^6.7.1
